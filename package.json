{
  "name": "orange-link",
  "private": true,
  "scripts": {
    "dev": "concurrently -n app,back -c cyan,green \"npm run dev --prefix orange-link-apollo\" \"npm run dev --prefix orange-link-back\"",
    "setup": "npm install && cd orange-link-apollo && npm install && cd ../orange-link-back && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
